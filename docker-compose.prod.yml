services:
  homepage:
    image: whyzzie/homepage:latest
    container_name: wysiecki-homepage
    expose:
      - "80"
    restart: unless-stopped
    depends_on:
      - api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.homepage.tls=true"
      - "traefik.http.routers.homepage.entrypoints=websecure"
      - "traefik.http.routers.homepage.tls.certresolver=cloudflare"
      - "traefik.http.routers.homepage.rule=Host(`wysiecki.de`) || Host(`www.wysiecki.de`)"
      - "traefik.http.services.homepage.loadbalancer.server.port=80"

  api:
    image: whyzzie/homepage-api:latest
    container_name: wysiecki-api
    restart: unless-stopped
    environment:
      - PORT=3000
      - SMTP_HOST=${SMTP_HOST:-}
      - SMTP_PORT=${SMTP_PORT:-465}
      - SMTP_USER=${SMTP_USER:-}
      - SMTP_PASS=${SMTP_PASS:-}
      - MAIL_TO=${MAIL_TO:-info@wysiecki.de}
      - MAIL_FROM=${MAIL_FROM:-}
      - TURNSTILE_SECRET=${TURNSTILE_SECRET:-}
      - TURNSTILE_SITE_KEY=${TURNSTILE_SITE_KEY:-}
      - ANALYTICS_TOKEN=${ANALYTICS_TOKEN:-}
      - ANALYTICS_SALT=${ANALYTICS_SALT:-}
      - ANALYTICS_DB_PATH=/data/analytics.db
    volumes:
      - analytics-data:/data
    expose:
      - "3000"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  analytics-data:

networks:
  default:
    external: true
    name: mvw-net

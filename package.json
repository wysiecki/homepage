{
  "name": "martin-wysiecki-homepage",
  "version": "1.0.0",
  "description": "Modern portfolio homepage for Martin von Wysiecki",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js && npx tailwindcss -i ./src/css/input.css -o ./dist/output.css --watch",
    "build": "mkdir -p dist && npx tailwindcss -i ./src/css/input.css -o ./dist/output.css --minify && node scripts/build.js",
    "lint": "eslint src/js/*.js",
    "format": "prettier --write 'src/**/*.{html,js,css,md}' 'scripts/**/*.js' '*.{js,json}'",
    "format:check": "prettier --check 'src/**/*.{html,js,css,md}' 'scripts/**/*.js' '*.{js,json}'"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.5",
    "prettier": "^3.8.1",
    "tailwindcss": "^3.4.0"
  }
}
